# Required for Chat when CHAT_PROVIDER=openai: a valid OpenAI API key.
# Chat uses a model that supports function/tool calling (e.g. gpt-4o, gpt-4-turbo, gpt-3.5-turbo).
# If you see "Error calling model. Verify the model exists, you have access, and it supports function/tool calling.",
# set OPENAI_API_KEY to a valid key and ensure DEFAULT_CHAT_MODEL (e.g. gpt-4o) is correct.
OPENAI_API_KEY=example-openai-api-key

# Use a different host port if 8000 is already in use (e.g. on shared hosting):
# DOCKER_API_PORT_MAPPING=127.0.0.1:8001:8000

# GITHUB_TOKEN=example-github-token

# RAGPI_API_KEY=example-api-key

# Allow browser requests from other origins (e.g. DevBrain AI on localhost:3000 or your domain)
# CORS_ENABLED=true
# CORS_ORIGINS=["http://localhost:3000","https://your-frontend-domain.com"]
